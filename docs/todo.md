以下 todo 不分先后顺序，但是注意有些 todo 存在依赖关系。todo 应该同步到需求分析说明书中，这里仅作为参考。

## 部分完成

3、声音特效，包括点击UI、炮弹爆炸、单位攻击之类的；—— 背景音乐已接入（`MusicPlayer`，分 starting / gaming 两套）；但点击UI、炮弹爆炸、单位攻击之类的具体音效还没做。
5、进入游戏后的菜单UI，以及UI美化；—— 欢迎界面、主菜单、FlatLaf、透明按钮、GIF 动态背景已做；其余界面继续美化。
6、动态效果。目前全是贴图；—— 射弹拖尾、爆炸环等程序化特效已有；单位本体仍是静态贴图。

## 已完成

13、兵种价格 + 关卡资金（FR-12）。单位定义新增 `cost`，关卡定义新增 `playerFunds`：>0 时启用资金制（仅受资金约束、不限数量，按价格自由组合，放置/移除实时扣减/返还，买不起置灰；`playerBudget` 此时仅作可用兵种名册），未给资金则退化为原固定配额制（向后兼容）。已加 `DeployFundsTest`。
15、结算原因说明（FR-27）。新增 `VictoryReason`（ANNIHILATION 全歼 / ATTRITION 损失超 90% / STALEMATE 僵持超时），`World` 记录并暴露 `victoryReason()`，结算界面据胜负与原因给出贴切标题 + 一句话说明。已加 `VictoryReasonTest`。
18、兵种简介（FR-13）。布兵阶段新增"兵种简介"按钮，弹窗列出可用兵种的移动域/生命/视野/攻击域/射程/伤害/价格。文本格式化拆到 `UnitInfoText`（无 Swing 依赖、可测），已加 `UnitInfoTextTest`。

## 待办

7、设计一些更合理的关卡。目前的关卡太唐了（level_07 现在还叫"地图测试"）；
11、潜艇应该能够浮出水面。相应的，其可打击目标和射程之类的也应同步切换；
12、玩家应该能够在局内实时调整AI的总体策略策略（在右边UI可以调），比如激进的，保守的，之类的。可以给玩家加一点参与感（ ；
17、关卡编辑器。每次都要手搓 .map 文件太麻烦了，有无更省事的办法。这个实现了之后，还能支持用户自定义关卡，增加可玩性；
